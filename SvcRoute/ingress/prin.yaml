apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingrss-prin
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    traefik.ingress.kubernetes.io/router.tls: true
spec:
  rules:
    - host: tools.memosa.cn
      http:
        paths:
          - path: /
            pathType: Exact
            backend:
              service:
                name: prin-svc
                port:
                  number: 80
tls:
- secretName: tls-memosa-cn